DATABASE_URL="postgresql://multiclin:multiclin@localhost:5435/multiclin?schema=public"
JWT_SECRET="troque-por-um-segredo-forte-antes-de-subir-para-producao"
PORT=3333

# Origem do frontend — usada pra montar o link de convite/redefinição de senha no e-mail
FRONTEND_URL=http://localhost:5173

# E-mail (convite de primeiro acesso, redefinição de senha, e depois RF07).
# Sem SMTP_HOST preenchido, o e-mail só é logado no console (ver lib/email.js) — dá pra testar sem provedor real.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Multiclin <no-reply@multiclin.example>"
